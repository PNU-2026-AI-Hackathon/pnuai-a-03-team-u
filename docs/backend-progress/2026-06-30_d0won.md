# 2026-06-30 작업 기록

## 구현한 것

- FastAPI 프로젝트 골격 ([#11](https://github.com/PNU-2026-AI-Hackathon/pnuai-a-03-team-u/pull/11))
  - `app/main.py` 앱 엔트리포인트, `/health`
  - `app/core/config.py` — `pydantic-settings` 기반 환경변수
  - `app/core/db.py` — SQLAlchemy `Base`, `TimestampMixin`, `get_db()`
  - Alembic 초기화, `migrations/env.py`가 모든 도메인 모델을 import해 `Base.metadata`에 등록
- 부산대 One-Stop 포털 크롤러 ([#12](https://github.com/PNU-2026-AI-Hackathon/pnuai-a-03-team-u/pull/12))
  - `pnu_session.py` — 로그인 + 메뉴 이동
  - `student_info.py`, `grades.py`, `graduation.py` — 학적부/성적/졸업요건 추출
  - `table_extract.py` — `<table>` 범용 추출 + `.b-row-item` 구조 추출
  - 본인 계정으로 학적부 / 졸업요건 / 전체성적 3개 실제 데이터 추출 확인
- 크롤러 raw 데이터 → DB 모델 매핑 ([#13](https://github.com/PNU-2026-AI-Hackathon/pnuai-a-03-team-u/pull/13))
  - `User`, `PortalCredential`, `UserAcademicProgram`, `StudentCourseRecord`, `Course`, `RequirementSet`, `GraduationAudit` 모델 추가
  - `pnu_normalizer.py` — `save_portal_credential`, `map_student_record`, `map_grades`, `map_graduation_requirement`
  - 포털 비밀번호는 `core/security.py`의 Fernet 암호화로 저장 (평문 저장 안 함)
- 백엔드 폴더 구조 정리 ([#14](https://github.com/PNU-2026-AI-Hackathon/pnuai-a-03-team-u/pull/14))
  - 도메인 모델을 `app/domains/{users, academics, courses}`로 통합
  - LLM/RAG/추천 관련 코드를 `app/ai/{llm, rag, recommendations, prompts}`로 재편
  - `app/data_ingestion` → `app/ingestion`으로 이름 변경
  - `app/api/` 신설 (라우터용)
  - `onestop_course_catalog.py` 수강편람 크롤러 추가

## 고친 것

- (해당 없음 — 이번 세션은 신규 구현 위주)

## 막혔던 부분 / 삽질

1. 로그인 레이어를 열어도 기본 활성 탭이 "스마트 로그인"이라 `#login_id`가 안 보임
   → `#idpwTab > a`를 먼저 클릭해야 함 (애니메이션 대기 필요, `wait_for_timeout(500)`)
2. 비밀번호 변경 주기가 지난 계정은 로그인 직후 `UpdatePassword` 페이지로 리다이렉트됨
   → "다음에 변경하기" 링크는 `href="javascript:onclick=changeNextPw();"` 형태라 클릭이 아니라
   `page.evaluate("changeNextPw()")`로 직접 호출해야 함
3. `selectMenu('menuCD')`는 AJAX가 아니라 실제 페이지 네비게이션
   → `page.expect_navigation()`으로 감싸야 함 (안 그러면 "Execution context was destroyed" 에러)
4. 학적부 기본정보(학번/이름/소속학과 등)는 `<table>`이 아니라
   `.b-row-item` > `.b-title-box`(라벨) + `.b-con-box`(값) 구조
   → `table_extract.extract_row_items()` 사용

## 아직 안 한 것 / 다음에 할 것

1. PostgreSQL 로컬 실행(Docker) + Alembic 마이그레이션 적용
2. 크롤러를 FastAPI 라우터로 노출 + 백그라운드 작업화
3. 사용자별 자격증명 입력 플로우 (회원가입/설정 화면 연동)
4. 비교과 활동 크롤러 (`my.pusan.ac.kr`)
5. `graduation_engine`(현 `domains/academics`)의 실제 판정 로직 (요건 규칙 시드 데이터 필요)
