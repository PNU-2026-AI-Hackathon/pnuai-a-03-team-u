# 2026-07-01 작업 기록

## 구현한 것

- 비교과 활동 공지사항 크롤러 ([#19](https://github.com/PNU-2026-AI-Hackathon/pnuai-a-03-team-u/pull/19))
  - `app/ingestion/crawlers/notice_board_sources.py` — 4개 엔진 설정(artclView/CMS Board/Pyxis JSON/job board)
  - `app/ingestion/crawlers/notice_board_crawler.py` — 7개 출처 크롤링, 90일 lookback 페이지네이션 멈춤, JSON 백업
  - 출처: swedu, uitc, pnucounsel, ctl (artclView POST), pusan_main (CMS Board GET), lib (Pyxis JSON API), job (li.tbody)
  - 90일 기준 878건 수집 확인
- `app/domains/activities/models.py` — 비교과 활동 도메인 모델
  - `Activity`: source, source_url, title, description, posted_date, deadline, category, embedding(pgvector 1536차원), is_pinned, views
  - `UserActivityRecommendation`: similarity_score + career_weight + recency_weight → final_score 캐시
  - source + source_url unique constraint, IVFFlat 코사인 유사도 인덱스
- `app/ingestion/normalizers/activity_normalizer.py` — NoticeRow → Activity upsert
  - 제목 키워드 기반 카테고리 자동 분류 (공모전/인턴십/취업/장학금/스터디/교내활동/강연특강/교육프로그램/도서관/상담)
  - 제목에서 마감일 자동 파싱 (`~7/6`, `(7/10)`, `7/15마감`, `6월 30일까지` 등)
  - embedding 컬럼은 건드리지 않음 (별도 배치 담당)
- `app/core/scheduler.py` — APScheduler 매일 00:00 KST 자동 크롤 + DB upsert
- `app/main.py` — FastAPI lifespan에 scheduler start/shutdown 연결
- `migrations/env.py` — activities 모델 import 추가

## 고친 것

- `app/main.py`: lifespan 추가 (기존엔 없었음)
- 머지됐던 `feat/pnu-portal-crawler` 브랜치 재생성 시도 → main의 domains/ 리팩토링과 구조 충돌 확인 후 PR 닫고 브랜치 삭제
- 로컬/원격 머지된 브랜치 전체 정리 (11개 삭제)

## 막혔던 부분 / 삽질

1. `lib.pusan.ac.kr/guide/notice` — Angular SPA라 정적 HTML 없음
   → Playwright 네트워크 캡처로 `pyxis-api/1/bulletin-boards/2/bulletins` JSON API 발견, requests로 직접 호출
2. `my.pusan.ac.kr` 비교과 개인화 페이지 — 로그인 필수 + UpdatePassword 우회 필요
   → 로그인 없이 접근 가능한 공개 게시판 7개로 방향 전환
3. artclView 게시판 제목에 "새글" 배지 텍스트(`<span class="new">`) 가 붙어 나옴
   → `badge.decompose()`로 span 제거 후 재크롤
4. `lib` 게시판 상세 URL 패턴 — href 없이 click 이벤트로 라우팅
   → Playwright로 실제 클릭 후 URL 확인 → `https://lib.pusan.ac.kr/guide/notice/{id}` 패턴 확인

## 아직 안 한 것 / 다음에 할 것

1. Supabase 연결 + `alembic upgrade head` (테이블 생성)
2. Activity 임베딩 생성 배치 (`ai/embeddings/activity_embeddings.py`)
3. 사용자 프로필 임베딩 생성 (User.department + career_goal 기반)
4. 추천 로직 (`ai/recommendations/`) — 임베딩 유사도 + career_goal 가중치 + 마감임박 정렬
5. 추천 API 엔드포인트 (`app/api/activities.py`)
6. `job.pusan.ac.kr`, `lib.pusan.ac.kr` 외 추가 출처 검토
