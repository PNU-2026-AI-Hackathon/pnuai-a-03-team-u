services:
  postgres:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: planu
    ports:
      - "5432:5432"
    volumes:
      - planu_local_pg:/var/lib/postgresql/data

volumes:
  planu_local_pg:
