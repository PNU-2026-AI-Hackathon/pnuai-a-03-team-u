# 이 파일은 "무엇에 직접 의존하는가"만 적는다. 실제 버전은 constraints.txt가 잠근다.
# 설치할 때 반드시 둘 다 넘길 것 — 안 그러면 매번 최신 버전이 깔린다:
#
#     pip install -r requirements.txt -c constraints.txt
#
# 여기에 패키지를 추가/삭제했으면 constraints.txt도 재생성해야 한다(그 파일 헤더 참고).

# 웹 프레임워크
fastapi
uvicorn[standard]

# DB / ORM
sqlalchemy
psycopg2-binary
alembic
pgvector

# 인증 / 보안
python-jose[cryptography]
bcrypt
python-multipart
# 엔드포인트별 레이트 리밋 (로그인 brute force, 챗 LLM 비용 폭탄 차단).
# 기본은 in-memory 저장소 — 워커 여러 개로 스케일아웃하면 RATE_LIMIT_STORAGE_URI에
# redis://를 지정해야 한도가 프로세스별로 쪼개지지 않는다. app/core/ratelimit.py 참고.
slowapi

# AI / RAG (LangChain)
langchain
langchain-openai
langchain-community
langchain-postgres
# 멀티 LLM 프로바이더 스왑용(ROADMAP_AGENT_MODEL="provider:model"). 쓰려는 프로바이더만
# 주석 해제하면 됨 — langchain init_chat_model이 해당 통합 패키지를 lazy import 한다.
# langchain-anthropic
# langchain-google-genai
anthropic
openai

# LLM 관측(트레이싱/평가). 팀 공유 셀프호스팅 인스턴스로 전송. 4.x는 OpenTelemetry 기반.
langfuse>=4.0,<5.0

# 크롤러
apscheduler
beautifulsoup4
requests
playwright

# OCR (성적표 이미지 처리)
pytesseract
pillow

# 환경설정 / 검증
pydantic-settings
python-dotenv
email-validator
