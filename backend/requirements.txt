# 웹 프레임워크
fastapi
uvicorn[standard]

# DB / ORM
sqlalchemy
psycopg2-binary
alembic
pgvector

# 인증 / 보안
python-jose[cryptography]
bcrypt
python-multipart
# 엔드포인트별 레이트 리밋 (로그인 brute force, 챗 LLM 비용 폭탄 차단).
# 기본은 in-memory 저장소 — 워커 여러 개로 스케일아웃하면 RATE_LIMIT_STORAGE_URI에
# redis://를 지정해야 한도가 프로세스별로 쪼개지지 않는다. app/core/ratelimit.py 참고.
slowapi

# AI / RAG (LangChain)
langchain
langchain-openai
langchain-community
langchain-postgres
# 멀티 LLM 프로바이더 스왑용(ROADMAP_AGENT_MODEL="provider:model"). 쓰려는 프로바이더만
# 주석 해제하면 됨 — langchain init_chat_model이 해당 통합 패키지를 lazy import 한다.
# langchain-anthropic
# langchain-google-genai
anthropic
openai

# LLM 관측(트레이싱/평가). Langfuse Cloud로 전송. 4.x는 OpenTelemetry 기반.
langfuse>=4.0,<5.0

# 크롤러
apscheduler
beautifulsoup4
requests
playwright

# OCR (성적표 이미지 처리)
pytesseract
pillow

# 환경설정 / 검증
pydantic-settings
python-dotenv
email-validator
